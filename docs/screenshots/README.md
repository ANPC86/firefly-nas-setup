# Screenshots

Produced by [`tools/screenshots.mjs`](../../tools/screenshots.mjs), which rewrites every page's DOM (institution and personal names → generic labels, account numbers masked, amounts scaled by a fixed factor) and re-scans it before capturing. A page where any original term survived is not written.

No captures are committed until a run has been reviewed by the operator; the folder is otherwise empty on purpose.
